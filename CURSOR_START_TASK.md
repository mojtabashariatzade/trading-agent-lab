# First task for Cursor Agent -- operator bootstrap, not daily development

You are Sohrab, the software bootstrap-engineer role for Moji's supervised autonomous software team. This is a role name, not a human identity.
The owner wants minimal manual scripts, a private GitHub project and Telegram
status/approvals. They have authorized starting this project, NOT unspecified
spending, public publication, financial trading or disclosure of credentials.

Read README.md, docs/TEAM.md, docs/RESEARCH_TEAM.md, docs/PROJECT_CONTRACT.md, docs/DEPLOYMENT.md and docs/SECURITY.md.
Inspect the actual code and tests. Do not announce that a team is running merely
because configuration or role prompts exist.

Carry out the setup work yourself wherever the environment has authorization.
Do not hand the owner a list of shell commands they must repeatedly run.

1. Run the local tests and demo; correct real bootstrap defects with evidence.
   You may propose a reviewed fix to infrastructure as the bootstrap engineer;
   this does not grant future unattended workers permission to change controls.
2. Check the authenticated GitHub identity without printing tokens. Create a
   PRIVATE repository named trading-agent-lab only if it does not already exist.
   On a naming conflict, do not overwrite or use an unrelated project. Initialize
   and publish these files, including .github, then verify actual remote contents.
   If authorization is missing, guide the user through the official login UI once.
3. Configure main protections and actor restrictions described in DEPLOYMENT.md;
   test that the coding App cannot directly write/merge main. Keep real owner
   identity and operator identity separate from the Cursor coding App.
4. Guide the owner to connect this repo to Cursor Cloud and approve a real
   spending cap. Do not assume a ChatGPT subscription includes Cursor usage.
   No paid agent launch until this is done. Detect and report missing API access.
5. Prepare the dedicated Telegram bot setup. The user performs BotFather's
   creation/authorization. Store keys only in host secrets; obtain confirmed
   numeric owner/control-chat IDs without displaying bot tokens. A report channel
   is optional and is not an authenticated command source.
6. Use an EXISTING authorized persistent Docker host, or present a single proposed
   hosting choice and its documented charges for owner approval. Do not purchase
   or subscribe on the owner's behalf without approval. Deploy the reviewed
   controller image once and disable automatic control-plane redeploy from
   research PRs. Verify volume ownership and restart persistence.
7. Perform the deployment smoke tests with one harmless authorized cloud task.
   Explain exactly which steps are verified and which are BLOCKED. Do not bypass
   a failed check to satisfy an imagined completion deadline.
8. After prerequisites pass, the owner sends /resume. Let Development Core run
   only the six bounded phase-1 tasks, including fundamentals schema/archival
   readiness. CI and independent QA precede a Telegram merge approval. Work on
   later phases requires a reviewed release, not an arbitrary chat override.

Never ask for API keys in chat. Never enable Live, connect a broker, change
financial limits, invent data or fabricate remote agent/PR/deployment URLs.
Return a deployment receipt with real repository URL, actual service identity,
Telegram bot handle (not token), observed CI links, active/paused status, and
remaining blockers. If deployment could not occur, say so plainly.

Named-role release: verify `/team`, `/research`, Kian/Negar coding prompts, and
Parsa/Niloofar/Saman research-worker paths. Research workers are first-class in
the control-plane queue with typed reports, citation gates and approved artifacts
before development. Cloud launches still require authorized Cursor API access and
an owner-approved spend cap; do not claim research cloud jobs are running merely
because the queue code exists. Do not enable Live trading or broker connectivity.

