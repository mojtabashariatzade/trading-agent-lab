"""Placeholder so the candidate suite is non-empty until a real task adds tests."""
import unittest


class CandidateSuitePlaceholderTests(unittest.TestCase):
    def test_candidate_suite_is_wired(self):
        self.assertTrue(True)


if __name__ == "__main__":
    unittest.main()
